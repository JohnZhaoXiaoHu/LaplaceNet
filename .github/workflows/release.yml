name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Push to release branch
        run: git push origin HEAD:release

      - name: Create release
        if: ${{ github.event_name != 'workflow_dispatch' }}
        id: release_tag
        uses: Lean365/release-tag@master
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          body: |
            Please refer to [CHANGELOG.md](https://github.com/Lean365/LaplaceNet/blob/master/CHANGELOG.md) for details.
